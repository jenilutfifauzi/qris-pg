# Salin ke .dev.vars untuk wrangler dev lokal
# Generate: openssl rand -hex 24
API_KEY=ganti-hex-random
# Opsional: secret terpisah utk HMAC callback (default: API_KEY)
# CALLBACK_SECRET=ganti-hex-juga
# Basic auth dashboard (publik-facing)
# DASH_USER=admin
# DASH_PASS=isi-password
